/**
 * Everything on the landing card. Swap `avatarSrc` for a real photo whenever
 * you like - drop the file in `public/` and point at it, e.g. "/us.jpg".
 */
export const INTRO = {
  /** Set to a path inside /public to show a real picture instead of the emoji. */
  avatarSrc: null as string | null,
  avatarAlt: "A little picture of us",
  /** Shown while `avatarSrc` is null. */
  avatarEmoji: "🧸",
  greeting: "Hey babe,",
  question: "want to go out on a date? ❤️",
  hint: "one of these buttons is a lot easier to click than the other",
  yesLabel: "YES ❤️",
};
